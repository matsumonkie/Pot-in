h1 Relationships

- @relationships.each do |relation_type, relations|
  h3 = relation_type
  table.table
    - relations.each do |relation|
      tr.tr_stripped
        td = relation.contact.firstname
        td = relation.contact.lastname      
        td.icon-edit = link_to t('actions.edit'), edit_user_relationship_path(current_user, relation)
        td.icon-delete = link_to t('actions.delete'), user_relationship_path(current_user, relation), data: {:confirm => 'Are you sure?'}, :method => :delete
    
    
br

= link_to 'New Relationship', new_user_relationship_path
